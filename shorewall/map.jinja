# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml "shorewall/os_family_map.yml" as os_family_map %}

{# get the settings for the os_family grain #}
{% set map = salt['grains.filter_by'](os_family_map, merge=salt['pillar.get']('shorewall:lookup')) %}
